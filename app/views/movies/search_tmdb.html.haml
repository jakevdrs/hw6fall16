%h1 The following movies containing '#{@search_terms}' were found on TMDb

= form_tag movies_add_tmdb_path, :method => :post, :id => 'add_form' do
    %table#movies
        %thead
            %tr
                %th{:class => @title_header} Movie Title
                %th Rating
                %th{:class => @date_header} Release Date
                %th Select
        %tbody
            - @movies.each do |movie|
                %tr
                    %td= movie[:title] 
                    %td= movie[:rating]
                    %td= movie[:release_date]
                    %td= check_box_tag "tmdb_movies[#{movie[:tmdb_id]}]", :id => 'checkbox_#{:tmdb_id}'
        
    = submit_tag 'Add Selected Movies'
= link_to 'Return to Movie List', movies_path